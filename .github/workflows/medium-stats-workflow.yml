name: Medium Stats Workflow

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily
  workflow_dispatch:

jobs:
  update-readme-with-medium-stats:
    name: Update README with Medium stats
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install node-fetch@2  # Using version 2.x which supports CommonJS
      
      - name: Fetch Medium Stats
        uses: actions/github-script@v7
        env:
          MEDIUM_TOKEN: ${{ secrets.MEDIUM_TOKEN }}
        with:
          script: |
            const fetch = require('node-fetch');
            
            try {
              const response = await fetch('https://api.medium.com/v1/me', {
                headers: {
                  'Authorization': `Bearer ${process.env.MEDIUM_TOKEN}`,
                  'Content-Type': 'application/json',
                  'Accept': 'application/json'
                }
              });
              
              if (!response.ok) {
                throw new Error(`HTTP error! status: ${response.status}`);
              }
              
              const data = await response.json();
              console.log('Medium API Response:', data); // Add logging for debugging
              
              // Update README with stats
              const fs = require('fs');
              let readme = fs.readFileSync('README.md', 'utf8');
              const statsSection = `\n## Medium Stats 📊\n- Followers: ${data.data.followersCount}\n`;
              
              // Replace existing stats section or add new one
              if (readme.includes('## Medium Stats 📊')) {
                readme = readme.replace(/## Medium Stats 📊[\s\S]*?(?=\n##|$)/, statsSection);
              } else {
                readme = readme.replace('## Latest Blog Posts 📚', `${statsSection}\n## Latest Blog Posts 📚`);
              }
              
              fs.writeFileSync('README.md', readme);
            } catch (error) {
              console.error('Error details:', error);  // Add detailed error logging
              core.setFailed(`Action failed with error: ${error}`);
            }

      - name: Commit and push if changed
        run: |
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update Medium stats [skip ci]" && git push)
